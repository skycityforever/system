version: '3.8'

services:
  polar-guard:
    build: .
    container_name: polar_guard
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./log:/app/log
    environment:
      - FLASK_ENV=production
      - TZ=Asia/Shanghai